ssh root@31.97.236.32

+1Nm19g7df7lvJbOPiN0

cd /home/xadventurebandarban/htdocs/xadventurebandarban.com



php artisan db:seed --class=PackageTypeSeeder
php artisan db:seed --class=PriceTypeSeeder
php artisan db:seed --class=RiderTypeSeeder
php artisan db:seed --class=PackagePriceSeeder



December 3, 2025 – Task Summary


1. Fix `editAtvUtv` controller to prepare `$dayPrices` in a flat array for proper Blade JS rendering.
2. Refactor ATV/UTV create and edit Blade JS into a single reusable Blade partial.
3. Ensure `day_prices` validation in `AtvUtvPackageRequest` handles null or empty prices correctly.
4. Update `edit.blade.php` to use `$dayPrices[$day]` safely and prevent undefined array key errors.
5. Test form submission for both create and edit to confirm correct JSON structure is sent for day-wise pricing.
